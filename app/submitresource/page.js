import SubmitResource from '@/components/SubmitResource'

export const metadata = {
  title: 'Submit a Resource - Reformed Chapter',
  description: 'Submit sermons, commentaries, devotionals and more to help build our library of Reformed biblical resources.',
}

export default function SubmitResourcePage() {
  return <SubmitResource />
}